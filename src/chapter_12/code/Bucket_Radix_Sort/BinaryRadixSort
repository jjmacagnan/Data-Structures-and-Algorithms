Algorithm binaryRadixSort(S)
    Input sequence S of b-bit integers
    Output sequence S sorted replace each element x of S with the item (0, x)

    for i ← 0 to b - 1
        replace the key k of each item (k, x) of S with bit x i of x

        bucketSort(S, 2)