R-14.6 Suppose we represent a graph G having n vertices and m edges with the edge list
structure. Why, in this case, does the insertVertex method run in O(1) time while
the removeVertex method runs in O(m) time?

O método insertVertex roda em tempo O(1), pois basta inserir o vertice na lista.
O método removeVertex roda em tempo O(m) em que m é o número de arestas, pois
para remover o vertice é preciso percorrer toda a lilsta de arestas e verificar quais
são incidem no vertice removido.